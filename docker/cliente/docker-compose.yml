services:
  cliente:
    build:
      context: .
      dockerfile: c.dockerfile
    ports:
      - "8080:8080/udp"  # <-- Permite que o host redirecione pacotes UDP para o contêiner
    environment:
      - SERVIDOR_ADDR=192.168.0.71

    stdin_open: true
    tty: true