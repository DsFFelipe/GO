services:
  servidor:
    build:
      context: .
      dockerfile: sv.dockerfile
    ports:
      - "8080:8080"
    environment:
      # O servidor precisa saber onde estão o Cliente e o Atuador na rede física
      - CLIENTE_ADDR=192.168.X.C:8080
      - ATUADOR_ADDR=192.168.X.A:8080