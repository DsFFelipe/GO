services:
  sensor1:
    build:
      context: .
      dockerfile: ss1.dockerfile
    environment:
      # O sensor precisa saber qual é o IP físico da máquina do Servidor
      - SERVIDOR_ADDR=192.168.0.71:8080